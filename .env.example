# URL del backend (origen, sin /api al final). Ver INSTRUCCIONES_FRONTEND_IA.md.
# Desarrollo: el proxy de Vite reenvía /api; solo necesitas esto si el backend no está en :3000.
# VITE_API_URL=http://localhost:3000

# Producción: obligatorio. Backend Zas Red Farmacias en Render.
VITE_API_URL=https://zas-red-farmacias-back.onrender.com
